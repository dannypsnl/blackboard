```agda
{-# OPTIONS --allow-unsolved-metas #-}
module sp-0002 where

open import MLTT.Spartan hiding (Type)
open import MLTT.List
open import UF.Base
```

這是讀 https://arxiv.org/abs/1809.08646 的筆記

如果我們拿到一個集合 $U$ 表示基本型別，可以定義 $\widetilde{U}$ 表示一組延拓，規則是

1. 屬於 $U$ 就是屬於 $\widetilde{U}$
2. 如果 $A, B$ 都屬於 $\widetilde{U}$，那 $A \times B$ 表示積型別也屬於 $\widetilde{U}$
3. 如果 $A, B$ 都屬於 $\widetilde{U}$，那 $A \Rightarrow B$ 表示函數型別也屬於 $\widetilde{U}$

```
data Lift (U : 𝓤 ̇ ) : 𝓤  ̇  where
  ι : U → Lift U
  _⟨×⟩_ : Lift U → Lift U → Lift U
  _⟨=>⟩_  : Lift U → Lift U → Lift U

infixl 40 _⟨×⟩_
infixl 20 _⟨=>⟩_
```

集合 $U$ 也有相應的 context，記成 $U^*$

```
Context : 𝓤 ̇ → 𝓤 ̇
Context U = List U
```

基礎定義的最後一個是 arity，記成 $U^\dag := U^* \times U$

```
_† : 𝓤 ̇ → 𝓤 ̇
U † = Context U × U
```

Clones 跟 Substitutions

```
a-clone-is-indexed-in-Γ-τ : (Lift (𝟙 {𝓤})) †
a-clone-is-indexed-in-Γ-τ = [] , ι ⋆

a-subst-is-indexed-in-Γ-Δ : {U : 𝓤 ̇ } → Context (Lift U) × Context (Lift U)
a-subst-is-indexed-in-Γ-Δ = [] , []
```
